rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary: Allow authenticated reads/writes for testing
    // TODO: Implement proper per-collection rules after verifying auth works
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
